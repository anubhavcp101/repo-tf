#Todo
Fill details for backend
 Create a storage account in a new resource group
Fill details for key vault secrets
 Create a key vault and secrets in that key vault in another resource group

Correct Image, size and custom data

custom data :
sudo apt update -y
sudo apt install apache2 -y
sudo systemctl start apache2
sudo systemctl enable apache2

system identity login extension
image details
variable and output files
ssh 

only output file is pending
